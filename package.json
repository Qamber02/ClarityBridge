{
  "name": "claritybridge",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:dashboard": "npm --workspace apps/dashboard run dev",
    "dev:zoom": "npm --workspace apps/zoom-panel run dev",
    "build": "npm --workspaces run build",
    "typecheck": "npm --workspaces run typecheck"
  }
}
